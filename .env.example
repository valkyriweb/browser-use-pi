# Copy to .env. Never commit real keys.
BROWSER_USE_API_KEY=
OPENROUTER_API_KEY=
# Used by examples/ (explicit `model:` argument).
MODEL=openrouter/openai/gpt-5.6-luna
# Used by the SDK itself when `model` is omitted; overrides DEFAULT_MODEL.
BROWSER_USE_MODEL=
BROWSER=local
DO_NOT_TRACK=1
